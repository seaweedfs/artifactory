# Copy to a private location on M01 and source it before running the lab CI.
# Do not commit credentials.

MONO_REPO=https://github.com/seaweedfs/seaweed-mono.git
MONO_REF=main

M02_HOST=testdev@192.168.1.184
M01_WORKDIR=/opt/rdma-lab-ci/work
M02_WORKDIR=/opt/rdma-lab-ci/work
ARTIFACT_DIR=$PWD/rdma-lab-runs

RDMA_PIPES=8
ENABLE_DC=0
DC_INITIATORS=4
